/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      remotePatterns: [
        {
          
          hostname: 'www.claudeusercontent.com',
        },
      ],
    },
  };
  

export default nextConfig;
